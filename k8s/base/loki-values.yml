loki:
  enabled: true
  persistence:
    enabled: false  # Keep it simple for local — no PVC needed

promtail:
  enabled: true
  config:
    clients:
      - url: http://loki:3100/loki/api/v1/push

grafana:
  enabled: false  # Already installed via prometheus-stack — don't install a second Grafana